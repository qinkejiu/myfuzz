# 项目目标与后续任务交接

更新日期：2026-09-08。本文件是 `feature/ibex-protocol-longrun` 分支的接续入口。
当前已完成真实 Ibex 契约转导器的官方 RFuzz 短测与重建重放；原计划 Task 14
正式收口、三组 300 秒实核/外设验收及 BOOM 验收仍暂缓、未完成。

本次以主工作区的历史交接文档为只读基线，在实际开发 worktree 新增本文。保留仍有效
的目标、实现节点和历史报告入口，取消旧的“从 P0 重跑”“Task 14 尚无短跑证据”等
过时行动指令。主工作区原件未修改。

## 1. 当前有效检查点：契约转导器 Task 8–9

实际开发位置：

```text
/home/qinkejiu/myfuzz/.worktrees/ibex-protocol-longrun
branch: feature/ibex-protocol-longrun
Task 8 implementation: 8180065
Task 8 independent-review fixes: a89b713
```

当前实施计划是
`docs/superpowers/plans/2026-09-08-contract-compiled-rfuzz-input-transducer.md`。
Task 8 实现和复审修复已提交；Task 9 负责实测证据与文档一致性。全分支独立复审、
最终验证和 push 由控制器继续完成，不能把本地文档提交描述为已推送远端。

入口是 `examples/real_ibex_rfuzz/run_example.py`，输入是同目录下的
`input/ibex-scratch.json`。真实 Ibex instruction/data OBI 经适配器和仲裁接入
`processor-memory-beat@1` 后端，再由生成的契约转导器处理请求。指令首次生成、
首次数据读取内容、握手选择、IRQ、NMI 和 debug request 都来自 RFuzz 周期记录。
每个测试用 `test_begin` 清空内存及 provenance；重复读取复用已生成字节，CPU 写入
按原值保留。固定测试头和周期记录共同形成可重放的测试输入。

当前配置是 RV32IMC、M 特权级、2 字节指令对齐，instruction/data 共享 `main`。
测试头固定 reset 2 周期、执行上限 200 周期、boot address 128、hart 0、合法编码
模式。布局 104 位，transport 每周期 16 字节、大端高位 payload、末尾 24 位 padding。
精确位区间、selector 修复、协议等待和同地址语义见
[示例 README](examples/real_ibex_rfuzz/README.zh-CN.md)。

此路径禁用固定 target。`personality` 只保留组合元数据，不能把本次运行描述为
scratch/GPIO/timer 外设功能执行。旧固定启动程序不是当前示例的指令来源。
编码合法性不保证程序不会 trap；例如访问未实现 CSR 时，真实 Ibex 可以输出诊断。

## 2. 已从磁盘核对的最终实测证据

最终运行目录是 `runs/examples/contract-rfuzz-review-5s`。Task 9 直接读取其
`summary.json`、`live/report.json`、`build/execution.json`、corpus manifest、
语料和 replay JSON，并核对磁盘二进制，未把实现报告摘要当成测量来源。

| 实测项目 | 最终运行结果 |
| --- | --- |
| RTL tests / 完成 feedback receipt | 124153 / 98557 |
| corpus / 同构建 replay | 23 / 23 |
| 执行周期 | 9932240 |
| backend request / completion | 249246 / 248306 |
| instruction request / response / 首次初始化 | 249246 / 248306 / 248306 |
| protocol / transducer / 总 errors | 0 / 0 / 0 |
| return code / remaining shared-memory segments | 0 / 0 |
| 请求时长 / 发出中断时间 | 5 秒 / 5.000014610002836 秒 |
| drain / 实际总时长 | 23.80154176299766 秒 / 28.802813402002357 秒 |
| 采样 RSS 峰值 | 108134400 bytes |
| 普通 RTL 诊断 | 4 行 |

`--seconds 5` 是第 5 秒请求客户端停止，客户端先完成当前批次，另有最多 60 秒排空
窗口。这不是严格 5 秒墙钟上限。报告中无需强制移除的 owned 段和遗留段均为零。
receipt 记录完成共享内存回复且去重的输入/覆盖哈希对，其数量不能替代执行测试数。
短输入可以留有尚未完成的尾请求，因此 request 与 completion 总数不要求相同。

本次语料的独立重建：
`runs/examples/contract-rfuzz-review-replay/rebuild_replay.json`，23 条通过。
较早保留在 `runs/examples/contract-rfuzz-5s` 的另一份语料也用新协议二进制重建重放：
`runs/examples/contract-rfuzz-original23-review-replay/rebuild_replay.json`，23 条通过。
旧语料兼容性证据不计入本次 live 的测试数。三份 replay 各 23 条均逐条核对 raw、
trace、coverage、layout、contract/transducer、header、control、simulator-input
和 replay key，且与相应磁盘二进制身份一致。

关键身份如下，`sha256:` 前缀省略：

| 身份 | SHA-256 |
| --- | --- |
| composition | `04456100aa3e8af5b743cb6b842353e58953323e27f8343c1c34d650a33209b3` |
| layout | `d23dd16f9cd98973407167d90bd3431d82c7736bf2519eef8c47934fad111d8a` |
| constraint = transducer | `6fcbb39a28ca4e157b2fde393a305feb87d1a192a0b6c1d9b7de74d6782e6d38` |
| header | `e75c5138856cc7b92c781edf93b9bf88fd017d04b65aeb7b09eb1d72cfa4c881` |
| live simulator | `e65ca99d81acc2ae36906b882944031c33ea3f215f236338807f382b8be256dd` |
| 本次语料重建 binary | `2292c69e3c61fe160fe5e240ea280ece98be7dcc8e77ad578484e8731347220a` |
| 原语料重建 binary | `52d1c7cdbc9ef71c188cdb0ece5b53b820300f75bcaf34af3a7b3a602c3b6cde` |

完整配置文件、raw/input、trace/coverage、replay key 及证据文件哈希收录于
[expected/bounded-result.json](examples/real_ibex_rfuzz/expected/bounded-result.json)。
其中 `representative_corpus_entry` 明确只表示 `entry_0001.json`；全部条目的身份
保留在由文件哈希绑定的 manifest/replay 中。独立构建 binary hash 和包含它的 replay
key 可以改变，原始输入、契约与覆盖反馈必须一致。

反馈种类是 `sampled-dut-signal-bit-events-u8-saturating`，只表示所选真实 DUT 信号位
和 backend 事件采样计数，不能解释为源码行或分支覆盖率。本次反馈包含 20 个外部
输入位和两个公开 backend 信号，不能仅凭反馈增益推断复杂指令或真实外设覆盖。

内部仿真协议是 `RFUZZ_READY 2`，请求和响应以 16 位小写十六进制的单调 uint64
编号关联，防止延迟的旧帧冒充新结果。编号不进入 DUT 输入；旧仿真二进制需重建。
重放同时核对原 manifest 的 `coverage_sha256` 和保留全部 trace 字节的
`trace_sha256`。普通 stdout 诊断有界保存，不计作反馈；协议帧错误仍失败。

`runs/` 被 Git 忽略，文档提交不包含这些大型本地产物。换机器时须恢复完整证据目录，
或按下节命令重新生成；只拿到 JSON 中的哈希不能执行重放。

## 3. 当前可执行命令与依赖

以下命令从实际 worktree 根目录执行。需要 Python 3、Verilator、C++ 编译器、配置
固定的 Ibex 源码和现有官方 RFuzz 客户端。

```bash
bash examples/real_ibex_rfuzz/commands.sh check
PYTHONPATH=src python3 examples/real_ibex_rfuzz/run_example.py --help
PYTHONPATH=src python3 examples/real_ibex_rfuzz/run_example.py inspect \
  --output runs/examples/contract-rfuzz-review-5s
```

已确认可用的客户端是 `runs/rfuzz_client_native_build/target/debug/kfuzz`，版本
`kfuzz 0.1.0`，SHA-256 为
`bbb72e52e60b1380cccd3b8c25febf0b6ccde51b17802d457ffadef43f87747b`。
计划中曾写的 `runs/rfuzz_client_native_build/release/rfuzz-client` 在当前环境不存在。
上游变异器没有全局 seed 参数；配置中的 composition seed 不是变异 seed，重放依赖
保存的实际输入和语料 lineage。未修改官方客户端源码、二进制或变异模式。

若要新运行，下面输出目录必须尚不存在；已存在时选择新的目录名：

```bash
bash examples/real_ibex_rfuzz/commands.sh compose runs/examples/contract-rfuzz-next-compose
bash examples/real_ibex_rfuzz/commands.sh test runs/examples/contract-rfuzz-next-5s 5
bash examples/real_ibex_rfuzz/commands.sh inspect runs/examples/contract-rfuzz-next-5s
bash examples/real_ibex_rfuzz/commands.sh replay \
  runs/examples/contract-rfuzz-next-5s runs/examples/contract-rfuzz-next-replay
```

`commands.sh` 设置 `PYTHONDONTWRITEBYTECODE=1`、`PYTHONPATH=src:.`、`JOBS=1`，
构建和测试使用 `nice -n15`。CLI 提供 `compose`、`test`、`inspect`、`replay`；
replay 的 `--output` 指向已有测试目录，`--build-output` 指向新建目录。

Task 9 最终验证命令：

```bash
PYTHONPATH=src python3 -m pytest tests/composition tests/isa \
  tests/examples/test_real_ibex_rfuzz_example.py \
  tests/integration/test_rfuzz_simulator.py tests/integration/test_rfuzz_live.py -q
PYTHONPATH=src python3 -m pytest tests -q
git diff --check
```

实际结果和逐项核对方法见 `.superpowers/sdd/contract-transducer-task-9-report.md`。
默认环境仍有官方客户端/实核 opt-in 测试跳过，不能把默认全量回归视作重新运行官方
客户端验收。真实 live 与两组重建证据在上节单独列出。全量项目必须限定 `tests/`；
裸 pytest 会误收集未跟踪第三方 checkout 中的脚本，并可能因可选 `bitstring`
缺失在收集时退出。`commands.sh full-tests` 已使用正确的项目范围。

## 4. 用户最终目标与必须保留的约束

目标是依赖感知、可自动组合 CPU 与外设的多组件 RTL 模糊测试系统，范围包括
Ibex、CVA6、BOOM，并可泛化到其他处理器。

- 接口来自源码事实和显式语义描述；适配器、接线、地址分配、约束和顶层由统一 IR
  推导，不使用 CPU 名称分支。未知类型、功能、协议或时序要求必须明确拒绝。
- RFuzz 字节布局、填充、输入约束与实际 DUT 驱动保持一致；区分指令编码合法、
  程序语义、处理器 trap、内存 provenance 和总线时序合法性。
- 最终随机选择三个已真实可执行的 CPU/外设组合，每个至少运行 300 秒，记录选择
  种子、源码、组合、覆盖率定义、各组件进展、错误、资源、退出和重建重放证据。
- 保持单构建/运行 worker、低优先级、默认不开波形、有界等待和内存监测。本次 live
  报告限制是软 512 MiB、硬 768 MiB，RSS 属于采样值，不是持续峰值的证明。
- 保留 `.superpowers/sdd/task-2-report.md` 既有修改和所有 `third_party/` 内容；
  不整体 stage、不删除参考 checkout、不回退用户文件、不结束无关进程。
- 不在主工作区重复实现或新建重复 worktree。接续时重新检查 status 与后台进程，
  不把历史“运行已结束”当作未来进程状态的保证。

## 5. 仍有效的历史实现与报告入口

以下是前序计划的历史节点，用于避免重复实现；它们不替代本轮转导器验收。
旧报告中的测试计数、失败状态和后续指令仅适用于各自记录时点。

| 历史工作 | 保留的实现和证据入口 |
| --- | --- |
| P0 小电路官方 RFuzz 闭环、退出/内存/重放修复 | `docs/reports/p0_rfuzz_resume_20260907.md` |
| elaborated 物理端口、显式 packed member 与 renderer | `docs/reports/p1_elaborated_port_reader_20260907.md`、`docs/reports/p1_explicit_member_annotations_20260907.md`、`docs/reports/p1_packed_container_renderer_20260907.md` |
| 有界 elaboration、immutable source snapshot | `docs/reports/p1_elaboration_runner_20260907.md`、`docs/reports/p1_elaboration_snapshot_integration_20260907.md` |
| CVA6 packed 类型、nonfatal warning 记录、gitlink pin、filelist/source closure | `docs/reports/p1_cva6_physical_reader_20260907.md`、`docs/reports/p1_cva6_recorded_warnings_20260908.md`、`docs/reports/p1_gitlink_source_provenance_20260908.md`、`docs/reports/p1_cva6_source_crawler_20260908.md` |
| 通用处理器边界与 beat 契约 | `docs/reports/p1_processor_boundary_cva6_20260908.md`、`docs/reports/p1_processor_memory_backend_contract_20260908.md` |
| OBI / AXI4 / TL-UL 适配器、packed runtime projection | `docs/reports/p1_obi_processor_memory_adapter_20260908.md`、`docs/reports/p1_axi4_processor_memory_adapter_20260908.md`、`docs/reports/p1_tl_ul_processor_memory_adapter_20260908.md`、`docs/reports/p1_packed_rfuzz_runtime_projection_20260908.md` |
| 处理器执行 IR、自动接线、连接式夹具、旧固定程序实核测试 | `docs/superpowers/plans/2026-09-08-processor-auto-wiring-and-real-cpu-acceptance.md`、`docs/reports/task16_processor_rfuzz_regression_20260908.md` |
| 当前契约转导器实核实现、诊断分帧和重放修复 | `.superpowers/sdd/contract-transducer-task-8-report.md` |

历史 Ibex/CVA6 固定程序执行快照和 `runs/task15-live-preflight-v3/` 只证明旧配置的
行为。CVA6 的 packed compiler provenance、受限 AXI4 适配等实现仍有效，但没有
本轮 CVA6 契约转导器 live 验收。不能沿用早期“CVA6 只有未编译参考源码”的状态，
也不能把旧固定程序 pass 计入新路径。

AXI4 仍是明确受限的单 outstanding、按序后端，不等于完整 burst/atomic/乱序支持；
TL-UL 也不等于 coherent TileLink A/B/C/D/E。历史原生 APB/Wishbone 和受限
AXI4-Lite 实现继续存在。较早三组 300 秒合成流量源与寄存器夹具运行不属于实核验收。

旧 `configs/campaigns/*-real-rfuzz.json` 固定程序配置尚未迁移，不能直接用
`scripts/run_real_cpu_campaigns.py` 把旧三种 personality 当成三个转导器外设组合；
新 candidate 要求会拒绝缺失契约模式的旧配置。不要把这条历史命令作为当前可运行
的正式长测入口。

## 6. 未完成项与下一轮顺序

1. 控制器完成本分支独立复审、最终验证后按用户请求 push 当前 feature 分支。
   本任务不代替该门槛，也未执行 push。
2. Task 14 正式收口仍暂缓、未完成，这是用户保留的独立门槛，与
   `.superpowers/sdd/progress.md` 和主工作区历史交接状态一致。
   当前有界短测证据不自动完成原计划 Task 14 的验收与正式收口；恢复后须按原计划
   逐项核对官方闭环、真实 CPU/外设执行证明、receipt 与重放身份及独立复审，再更新账本。
3. 正式 3×300 秒测试仍暂缓、未执行。恢复后先确定确实执行 CPU 和外设的配置及
   路由，迁移并验证 campaign 输入，再选择三组真实可执行组合，串行长测、按期
   保存检查点，并对每组全部语料独立重建重放。改变 personality 元数据不算外设验收。
4. BOOM 按用户要求继续暂缓。恢复时从已有隔离参考 checkout 和 blocker 证据接续，
   核对固定 BOOM/Chipyard pin、生成配置、完整源码闭包和 Java/SBT/Mill 工具链；
   先确认实际生成边界，再决定通用协议支持，不能凭 BOOM 源码已下载宣称真实执行。
5. 其他 CPU、统一 instruction/data 路由、其他 ISA 扩展和实际外设都需要独立验收。
   当前示例证明的是 Ibex 分离 OBI 路径；统一路由仍失败关闭，A/F/D 等不能仅凭 raw
   模式宣称指令编码约束已实现。任意长输入可能触发容量或 provenance 错误；本次
   零错误短测不证明所有随机输入永远无错。
6. 保留最终研究目标中的协议契约、常用组件清单和各 CPU ISA 编码资料需求；扩展前
   核对仓库实际覆盖范围，不能把参考文档数量当成协议/指令实现数量。

下一轮先读取本文、当前计划、Task 8/9 报告和示例说明，再核对实际工作树与本地证据。
从尚未完成项接续，不从历史 P0 失败记录重新开始，也不把短测或夹具结果合并成
项目全部完成的结论。
